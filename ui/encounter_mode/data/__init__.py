"""Data-only tables for Encounter Mode.

This package holds large static mappings (keywords, layout coordinates, etc.) so
render/logic modules stay smaller and easier to navigate.

Guideline: keep modules here free of Streamlit/UI side effects.
"""
